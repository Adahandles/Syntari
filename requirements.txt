# Syntari Programming Language Requirements
#
# NOTE: This file is maintained for backwards compatibility only.
# All dependencies are now managed in pyproject.toml.
#
# To install Syntari with all dependencies, use:
#   pip install -e ".[web]"
#
# This will install:
# - Core dependencies (pytest, black, flake8, mypy, pytest-asyncio)
# - Web extras (aiohttp, aiohttp-cors)

# For backwards compatibility, you can still install from this file:
-e .[web]
